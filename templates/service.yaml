{{/* ---------------- API SERVICES ---------------- */}}
{{- range $.Values.apps.api.services }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .name }}
  namespace: {{ $.Values.Namespace }}
  labels:
    app: {{ .name }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: {{ .name }}
{{- end }}


{{/* ---------------- UI SERVICES ---------------- */}}
{{- range $.Values.apps.ui.services }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .name }}
  namespace: {{ $.Values.Namespace }}
  labels:
    app: {{ .name }}
    ui-theme: {{ $.Values.apps.ui.config.theme | quote }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: {{ .name }}
{{- end }}
